package dmon.SSHOP_springboot_backend.enums;

public enum StatusEnum {
    ;
    public enum Seller { REGISTERING, REVIEWING, LIVE, DEACTIVATED, SUSPENDED, CLOSED, DELETED,  }

    public enum Product { DRAFT, REVIEWING, LIVE, DEACTIVATED, SUSPENDED, DELETED, }

    public enum Sku { LIVE, DEACTIVATED, }

}
